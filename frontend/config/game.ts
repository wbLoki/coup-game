export type GameConfig = typeof gameConfig;

export const gameConfig = {
    coup: {
        cards: {
            empty: 0,
            inquisitor: 100,
            contessa: 200,
            captain: 300,
            ambassador: 400,
            assassin: 500,
            duke: 600,
        },
    },
};
